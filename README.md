# CozyForecast
A pixel-art weather companion built with vanilla JavaScript, integrating OpenWeather API for real-time weather data, featuring conditional UI rendering based on API responses, dynamic DOM manipulation, native internationalization (Intl.DisplayNames), and custom animations using requestAnimationFrame.
